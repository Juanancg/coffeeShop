#include "tests_OrderManager.h"

TEST(tests_OrderManager, RegisterOrder)
{
    OrderManager order;
    // order.RegisterOrder();
}

TEST(tests_OrderManager, CancelOrder)
{
    OrderManager order;
    order.CancelOrder(1);
}